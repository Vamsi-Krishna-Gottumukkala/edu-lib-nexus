{
  "name": "edulib-rfid-bridge",
  "version": "1.0.0",
  "description": "RFID bridge — reads Arduino serial port and writes to Supabase",
  "type": "module",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js"
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^16.0.0",
    "serialport": "^12.0.0"
  }
}
